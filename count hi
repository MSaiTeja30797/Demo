public int countHi(String str) {
  String st="";
  int count=0;
  for(int i=0;i<str.length()-1;i++){
    st=str.substring(i,i+2);
    if(st.equals("hi"))
    {
      count++;
    }
  }
  return count;
}
